spring.datasource.url=${DB_VOLLMED_URL}
spring.datasource.username=${DB_VOLLMED_USER}
spring.datasource.password=${DB_VOLLMED_PASSWORD}

spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false